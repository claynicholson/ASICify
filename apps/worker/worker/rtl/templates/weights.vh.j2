// Hardwired constants generated by ASICify.
//
// Synthesis (yosys, Vivado) folds these into multiplier inputs and
// memory primitives. The Python reference (reference.py) and the cocotb
// testbench (tb_top.py) use the same numbers in the same units, so the
// generated RTL is bit-exact.

// ===== Linear layers =====
{% for view in linear_views %}
{% if view.weights_decl %}
{{ view.weights_decl }}

{% else %}
// {{ view.name }}: no quantized weights available.
{% endif %}
{% endfor %}

// ===== LayerNorms =====
{% for view in layernorm_views %}
{{ view.weights_decl }}

{% endfor %}

// ===== Embeddings =====
{% for view in embedding_views %}
{{ view.weights_decl }}

{% endfor %}

// ===== Softmax LUT (used by attention) =====
// 256-entry exp(d) LUT for d in [-255, 0], output in Q15.
{{ softmax_lut_decl }}

